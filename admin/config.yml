backend:
  name: github
  repo: YasminKhawaja/test-site
  branch: main
  auth_endpoint: https://test-site-three-ebon.vercel.app/api/auth

site_url: https://test-site-three-ebon.vercel.app

media_folder: "images"

collections:
  - name: "pages"
    label: "Pages"
    folder: "pages"
    create: true
    slug: "{{slug}}"
    fields:
      - { label: "Title", name: "title", widget: "string" }
      - { label: "Content", name: "body", widget: "markdown" }
